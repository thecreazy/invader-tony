/**
 * End page — Game Over or Win result screen.
 * Accepts final score, shows name input for leaderboard submission.
 */

/** @type {HTMLElement | null} */
let root = null;

/**
 * Mounts the end page into the given container.
 * @param {HTMLElement} container
 */
export function mount(container) {
  // TODO: implement
}

/**
 * Unmounts the end page and cleans up event listeners.
 */
export function unmount() {
  // TODO: implement
}

export const EndPage = { mount, unmount };
