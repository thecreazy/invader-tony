/**
 * Home page — title screen with arcade cabinet style.
 * Shows the game title, Start and Leaderboard options.
 * Mounts/unmounts its own DOM nodes.
 */

/** @type {HTMLElement | null} */
let root = null;

/**
 * Mounts the home page into the given container.
 * @param {HTMLElement} container
 */
export function mount(container) {
  // TODO: implement
}

/**
 * Unmounts the home page and cleans up event listeners.
 */
export function unmount() {
  // TODO: implement
}

export const HomePage = { mount, unmount };
