/**
 * Leaderboard page — displays top 10 scores from localStorage (API-ready).
 * Reads from the leaderboard service and renders the table.
 */

/** @type {HTMLElement | null} */
let root = null;

/**
 * Mounts the leaderboard page into the given container.
 * @param {HTMLElement} container
 */
export function mount(container) {
  // TODO: implement
}

/**
 * Unmounts the leaderboard page and cleans up.
 */
export function unmount() {
  // TODO: implement
}

export const LeaderboardPage = { mount, unmount };
